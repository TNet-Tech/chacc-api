from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from .context_factory import get_async_db, get_db

router = APIRouter()


# Example: Import models from the module package using relative imports
# from .models import {{module_name_pascal}}Item, {{module_name_pascal}}Create, {{module_name_pascal}}Response

# Example: Add your module endpoints here
# @router.post("/", response_model={{module_name_pascal}}Response)
# async def create_item(item: {{module_name_pascal}}Create, db: AsyncSession = Depends(get_async_db)):
#     # Implementation here
#     pass
